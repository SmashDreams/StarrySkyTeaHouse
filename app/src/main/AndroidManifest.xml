<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <permission
        android:name="com.bird.launcher.permission.READ_SESSION"
        android:protectionLevel="signature" />

    <uses-permission android:name="com.bird.starryskysudoku.permission.READ_RESULTS" />

    <queries>
        <provider android:authorities="com.bird.starryskysudoku.provider" />
    </queries>

    <application
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Launcher">
        <provider
            android:name=".session.SessionProvider"
            android:authorities="com.bird.launcher.provider"
            android:exported="true"
            android:readPermission="com.bird.launcher.permission.READ_SESSION" />

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
