<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <permission
        android:name="com.bird.starryskyteahouse.permission.READ_SESSION"
        android:protectionLevel="signature" />

    <uses-permission android:name="com.bird.starryskysudoku.permission.READ_RESULTS" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <queries>
        <package android:name="com.bird.starryskysudoku" />
        <provider android:authorities="com.bird.starryskysudoku.provider" />
    </queries>

    <application
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.StarrySkyTeaHouse">
        <provider
            android:name="com.bird.starryskyteahouse.session.SessionProvider"
            android:authorities="com.bird.starryskyteahouse.provider"
            android:exported="true"
            android:readPermission="com.bird.starryskyteahouse.permission.READ_SESSION" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.bird.starryskyteahouse.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/provider_paths" />
        </provider>

        <activity
            android:name="com.bird.starryskyteahouse.RegisterActivity"
            android:exported="false" />

        <activity
            android:name="com.bird.starryskyteahouse.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
